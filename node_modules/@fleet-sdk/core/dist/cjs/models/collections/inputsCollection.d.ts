import { Amount, Box, BoxId } from "@fleet-sdk/common";
import { ErgoUnsignedInput } from "../ergoUnsignedInput";
import { Collection } from "./collection";
export declare class InputsCollection extends Collection<ErgoUnsignedInput, Box<Amount>> {
    constructor();
    constructor(box: Box<Amount>);
    constructor(boxes: Box<Amount>[]);
    protected _map(input: Box<Amount> | ErgoUnsignedInput): ErgoUnsignedInput;
    protected _addOne(box: Box<Amount>): number;
    remove(boxId: BoxId): number;
    remove(index: number): number;
}
