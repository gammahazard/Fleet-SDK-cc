import { SelectionTarget } from "../builder/selector/boxSelector";
export declare class InsufficientInputs extends Error {
    readonly unreached: SelectionTarget;
    constructor(unreached: SelectionTarget);
}
