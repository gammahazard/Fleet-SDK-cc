import { BoxAmounts, OneOrMore } from "@fleet-sdk/common";
import { OutputBuilder } from "../../builder/outputBuilder";
import { SelectionTarget } from "../../builder/selector/boxSelector";
import { Collection } from "./collection";
export declare class OutputsCollection extends Collection<OutputBuilder, OutputBuilder> {
    constructor(outputs?: OneOrMore<OutputBuilder>);
    protected _map(output: OutputBuilder): OutputBuilder;
    remove(output: OutputBuilder): number;
    remove(index: number): number;
    clone(): OutputsCollection;
    sum(basis?: SelectionTarget | BoxAmounts): BoxAmounts;
}
